import React from 'react';
import classes from './component.module.scss';

const LoaderAnimation = () => (
  <div className={classes.LoaderAnimation} sr-only="Loading" />
);

export default LoaderAnimation;
